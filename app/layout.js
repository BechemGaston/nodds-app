import "./globals.css";

export const metadata = {
  title: "Nodds — one system for everything you keep",
  description:
    "Nodds builds small, purpose-built tools for the records people end up tracking in notebooks, spreadsheets, and memory.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link
          href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600&family=Inter:wght@400;500;600;700&display=swap"
          rel="stylesheet"
        />
      </head>
      <body>{children}</body>
    </html>
  );
}